# x402 market actions — 2026-08-25

Implemented in this branch:

- Add a low-cost `youtube-transcript-basic` tier intended to compete in price-sensitive discovery queries.
- Add `youtube-transcribe` discovery alias.
- Add `youtube-transcript-api` discovery alias.
- Keep the full transcript product positioned separately from the basic text-only tier.

Recommended pricing intent:

- `youtube-transcript-basic`: $0.0011 — text only.
- `youtube-transcript`: keep $0.004 — richer output/timestamps/language/formats.

This file records the market-driven product changes so they can be verified independently from research notes.
