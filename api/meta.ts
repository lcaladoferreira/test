import { agentCard, llms, openapi, publicBase, skill, x402Manifest } from "../src/meta.js";
import { NETWORK, PRICE_USDC, SERVICE_NAME, VERSION } from "../src/config.js";

export default async function handler(req:Request){const u=new URL(req.url);const doc=u.searchParams.get("doc")||"index";const base=publicBase(req);if(doc==="health")return Response.json({ok:true,service:SERVICE_NAME,version:VERSION,network:NETWORK,price:`$${PRICE_USDC}`,time:new Date().toISOString()},{headers:{"cache-control":"no-store"}});if(doc==="openapi")return Response.json(openapi(base));if(doc==="x402")return Response.json(x402Manifest(base));if(doc==="agent")return Response.json(agentCard(base));if(doc==="skill")return new Response(skill(base),{headers:{"content-type":"text/markdown; charset=utf-8"}});if(doc==="llms")return new Response(llms(base),{headers:{"content-type":"text/plain; charset=utf-8"}});const html=`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${SERVICE_NAME}</title><style>body{font:16px ui-monospace,monospace;max-width:900px;margin:60px auto;padding:0 20px;color:#111}code,pre{background:#f4f4f4;padding:3px 6px}pre{padding:18px;overflow:auto}a{color:#0645ad}</style></head><body><h1>${SERVICE_NAME}</h1><p>Real-time web extraction and normalization for autonomous AI agents.</p><p><b>$${PRICE_USDC} USDC / successful call</b> · x402 V2 · Base mainnet</p><p>HTTP <code>POST /api/extract</code> · MCP <code>POST /mcp</code> · tool <code>extract_web</code></p><p><a href="/openapi.json">OpenAPI</a> · <a href="/.well-known/x402.json">x402 manifest</a> · <a href="/SKILL.md">SKILL.md</a> · <a href="/llms.txt">llms.txt</a> · <a href="/.well-known/agent-card.json">agent card</a> · <a href="/health">health</a></p><pre>POST /api/extract\nContent-Type: application/json\n\n{\n  "url": "https://example.com/article",\n  "format": "json"\n}</pre><p>No signup. No API key. Autonomous x402 payment.</p></body></html>`;return new Response(html,{headers:{"content-type":"text/html; charset=utf-8"}})}
